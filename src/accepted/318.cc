#include <bits/stdc++.h>
using namespace std;

bool solve(int id) {
   int n, m; cin >> n >> m;
   if (!n && !m) return false;
   
   int key[500];
   iota(key, key+500, 1);

   
      
   return true;
}

int main() {
   int id = 1;
   while (solve(id++))
      ;
   

}
