#include <bits/stdc++.h>
using namespace std;

int main() {

   int s, b;
   cin >> s >> b;
   while (s != 0 && b != 0) {

      list<int> sol;
      sol.resize(s);
      iota (sol.begin(), sol.end(), 1); 
      
      // loss reports
      for (int i = 0; i < b; i++) {

	 int L, R; cin >> L >> R;

	 
	 
	 
      }

      


      
      
      cin >> s >> b;
   }
   

   return 0;
}
