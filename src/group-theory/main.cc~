#include <bits/stdc++.h>
using namespace std;

int main() {

   int A[] = { 4, 8, 12, 16 };

   int C[4][4] = {};
   for (int i = 0; i < 4; i++) {
      for (int j = 0; j < 4; j++) {
	 C[i][j] = (A[i] * A[j]) % 20;
      }
   }

}
